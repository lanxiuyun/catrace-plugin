# 作息回顾（heatmap）

Dashboard 过 0 点只显示今天。本插件在插件中心用日历翻日期，读宿主已经落库的 `records`。

三种视图：
- **环形表盘**：24 小时手表环，悬停看时段
- **单轴时间线**：一整天一条轴
- **作息流卡片**：按时段列表

紫=专注活跃，绿=休息睡眠，灰=未记录。需要带 `plugin.activity.getRecords` 的 Catrace。默认关闭。
