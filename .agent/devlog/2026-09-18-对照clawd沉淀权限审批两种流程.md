# 2026-09-18 对照 clawd 沉淀权限审批两种流程

## 会话目标

把 clawd-on-desk 的 PermissionRequest 实现写成 agent-notify 可对照的现行知识，避免把 `AskUserQuestion` 当成普通工具允许。

## 完成项

- 写 feature：普通工具 `allow`/`deny` vs human-question elicitation（选项可点、Other、多题、index answers → `updatedInput`）
- 标明 agent-notify 现状：`finishPerm()` 仍只回 `behavior`

## 待开发

- 权限卡分两条 UI：工具批准 vs 问答表单
- sidecar 校验答案并回 `updatedInput`

## 关键文件变更

| 文件 | 变更 |
|------|------|
| `.agent/features/agent-notify/权限审批-clawd的两种批准流程-普通工具allow-deny与AskUserQuestion多问题回传.md` | clawd 对照与回包结构 |
| `.agent/features/agent-notify/README.md` | 子文档索引 |
| `.agent/manifest.yaml` | 索引与 current.next |
