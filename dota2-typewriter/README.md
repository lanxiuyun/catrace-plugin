# Dota2 暗黑狂欢打字机

启用后，设置里打开 **显示卡片** 才出右下角小窗。开始后循环往前台打 `a`→`z`，给暗黑狂欢打字机用。

- 间隔是打完 **一轮** a–z 的总时间，默认 100ms，不是每个字母隔这么久。
- 卡片待机紫、打字中绿。上面开始（开始后变暂停），下面结束。
- 结束或点 × 关卡，并关掉「显示卡片」。
- Windows。需要本机 Node.js。默认关闭。
